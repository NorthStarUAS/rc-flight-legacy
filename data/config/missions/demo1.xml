<?xml version="1.0"?>

<PropertyList>

  <enable type="bool">true</enable>

  <!-- global tasks: each of these are run every iteration -->
  <global-tasks>
    <task>
      <name>home-manager</name>
    </task>

    <task>
      <name>lost-link</name>
      <timeout-sec>10</timeout-sec>
      <push-task>circle-coord</push-task>
    </task>

    <task>
      <name>throttle-safety</name>
      <min-alt-agl-ft>100</min-alt-agl-ft>
    </task>
  </global-tasks>

  <!-- sequential tasks: only the front item in the fifo queue is run -->
  <sequential-tasks>
    <task>
      <name>bungee-launch</name>
      <finish-alt-agl-ft>150</finish-alt-agl-ft>
    </task>

    <task>
      <name>route</name>
      <route include="../routes/bowtie-300m.xml" />
      <timed>0</timed>
      <loop>0</loop>
    </task>
  </sequential-tasks>

  <!-- standby tasks: a list of tasks that are initialized and configured -->
  <!-- and could be moved to the global or sequential task lists at any -->
  <!-- time. -->
  <standby-tasks>
    <task>
      <name>circle-coord</name>
      <coord-path>/navigation/startup</coord-path>
      <bank-deg>20</bank-deg>
      <altitude-agl-ft>500</altitude-agl-ft>
      <speed-kt>40</speed-kt>
    </task>

    <task>
      <name>flyto-coord</name>
      <coord-path>/navigation/startup</coord-path>
      <altitude-agl-ft>500</altitude-agl-ft>
      <speed-kt>40</speed-kt>
    </task>
  </standby-tasks>

</PropertyList>
